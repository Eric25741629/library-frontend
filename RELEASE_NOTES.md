# Release: v0.86.2beta

發布日期: 2026-01-12

狀態: Beta（可能有 bug）

重點說明：
- 調整前端 `/api/return` 呼叫，確保 `books` 陣列內包含 `title` 與 `image_url`，以避免後端重複查詢 SIP2。
- 修正 admin 面板列表的滾動與表頭固定問題，並讓垂直捲軸常駐顯示以利觸控操作。
- 新增後台顯示版本字串（由 `shared.APP_VERSION` 提供）。

已知風險/注意事項：
- 此為 Beta 釋出，部份流程（例如硬體狀態轉換或邊緣 race condition）可能還會暴露日誌訊息或需要進一步微調。
- 若在實機測試中發現機器在 `put` 或 `homing` 時停留於非預期狀態，請提供相關 `machine.log` 以利診斷。

回滾建議：
- 若遇到嚴重問題，可回滾到 `v0.86` 分支（請確認本地資料庫備份）。

---
請於測試完成後回報，我會協助合併或修正後續小版本（例如 v0.86.2 正式版）。